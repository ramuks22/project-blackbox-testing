[tool.black]
line-length = 100
target-version = ["py38"]
include = "\\.pyi?$"
extend-exclude = """
(
  /(\\.git|\\.venv|node_modules|target|blackbox-reports)/
)
"""

[tool.ruff]
line-length = 100
target-version = "py38"
exclude = ["node_modules", "target", "blackbox-reports"]

[tool.ruff.lint]
select = ["E", "F", "I"]
ignore = ["E501"]
